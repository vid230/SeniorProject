@page "/add-course"

@using AubgAcademicTracker.Models
@using AubgAcademicTracker.Services

@inject CourseService CourseService
@inject NavigationManager Navigation
@inject SemesterService SemesterService

<PageTitle>Add Course</PageTitle>

<h1>Add Course</h1>

<p>Enter the information for the course.</p>

<EditForm Model="@course" OnValidSubmit="@SaveCourse">
    <DataAnnotationsValidator />

    <ValidationSummary />

    <div class="mb-3">
        <label class="form-label">Course Code</label>

        <InputText class="form-control" @bind-Value="course.Code" />

        <ValidationMessage For="@(() => course.Code)" />
    </div>

    <div class="mb-3">
        <label class="form-label">Course Name</label>

        <InputText class="form-control" @bind-Value="course.Name" />

        <ValidationMessage For="@(() => course.Name)" />
    </div>

    <div class="mb-3">
        <label class="form-label">Credits</label>

        <InputNumber class="form-control" @bind-Value="course.Credits" />

        <ValidationMessage For="@(() => course.Credits)" />
    </div>

    <div class="mb-3">
        <label class="form-label">Grade</label>

        <InputSelect class="form-select" @bind-Value="course.Grade">
            <option value="">-- Select grade --</option>

            @foreach (string grade in grades)
            {
                <option value="@grade">@grade</option>
            }
        </InputSelect>

        <ValidationMessage For="@(() => course.Grade)" />
    </div>

    <div class="mb-3">
        <label class="form-label">Semester</label>

        <InputSelect class="form-select" @bind-Value="course.SemesterId">
            <option value="0">-- Select semester --</option>

            @foreach (Semester semester in semesters)
            {
                <option value="@semester.Id">
                    @semester.DisplayName
                </option>
            }
        </InputSelect>

        <ValidationMessage For="@(() => course.SemesterId)" />
    </div>

    <button type="submit" class="btn btn-primary">
        Save Course
    </button>

    <button type="button" class="btn btn-secondary ms-2" @onclick="Cancel">
        Cancel
    </button>
</EditForm>

@code {

    private Course course = new();

    private readonly string[] grades =
    {
        "A",
        "A-",
        "B+",
        "B",
        "B-",
        "C+",
        "C",
        "C-",
        "D+",
        "D",
        "F"
    };

    private async Task SaveCourse()
    {
        await CourseService.AddCourseAsync(course);

        Navigation.NavigateTo("/courses");
    }

    private void Cancel()
    {
        Navigation.NavigateTo("/courses");
    }

    private List<Semester> semesters = new();

    protected override async Task OnInitializedAsync()
    {
        semesters = await SemesterService.GetSemestersAsync();
    }
}